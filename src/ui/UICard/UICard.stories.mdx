import { Meta, Story, Preview } from "@storybook/addon-docs";
import ContentLoader from "react-content-loader";
import ChartsProvider from "../../components/ChartsProvider/ChartsProvider";
import UIChartPlaceholder from "../UIChartPlaceholder/UIChartPlaceholder";
import UIText from "../UIText/UIText";
import UILegends from "../UILegends/UILegends";
import UICard from "./UICard";

<Meta
  title="UI/UICard"
  component={UICard}
  decorators={[
    (storyFn) => (
      <ChartsProvider>
        <div style={{ width: "736px" }}>{storyFn()}</div>
      </ChartsProvider>
    ),
  ]}
/>

# UICard

## Examples

<Preview>
  <Story name="withTitle">
    <UICard isLoading={false}>
      <UICard.Header title="Card with Title" />
    </UICard>
  </Story>
</Preview>

<Preview>
  <Story name="withSubtitle">
    <UICard isLoading={false}>
      <UICard.Header
        title="Card with Title"
        subtitle="A long description goes in this part"
      />
    </UICard>
  </Story>
</Preview>

<Preview>
  <Story name="withPlaceholder">
    <UICard isLoading={true}>
      <UICard.Header
        title="Card with Title"
        subtitle="A long description goes in this part"
      >
        <UILegends
          legendsData={[
            { color: "supportive500", label: "Avg Legend 1", shape: "line" },
            { color: "primary500", label: "Avg Legend 2", shape: "dot" },
          ]}
        />
      </UICard.Header>
      <UICard.Placeholder>
        {(phDimensions) => (
          <UIChartPlaceholder
            dimensions={phDimensions}
            hasBars={true}
            hasLegends={true}
            hasTitle={true}
            hasSubtitle={true}
          />
        )}
      </UICard.Placeholder>
    </UICard>
  </Story>
</Preview>

<Preview>
  <Story name="withLegends">
    <UICard isLoading={false}>
      <UICard.Header
        title="Card with Title"
        subtitle="A long description goes in this part"
      >
        <UILegends
          legendsData={[
            { color: "supportive500", label: "Avg Legend 1", shape: "line" },
            { color: "primary500", label: "Avg Legend 2", shape: "dot" },
          ]}
        />
      </UICard.Header>
    </UICard>
  </Story>
</Preview>

<Preview>
  <Story name="withContent">
    <UICard isLoading={false}>
      <UICard.Header
        title="Card with Title"
        subtitle="A long description goes in this part"
      />
      <UICard.Content>
        <UIText tag="body2">Here goes a graph.</UIText>
      </UICard.Content>
    </UICard>
  </Story>
</Preview>

<Preview>
  <Story name="withoutHeader">
    <UICard isLoading={false}>
      <UICard.Content>
        <UIText tag="body2">Here goes a graph.</UIText>
      </UICard.Content>
    </UICard>
  </Story>
</Preview>

<Preview>
  <Story name="withAllSections">
    <UICard isLoading={false}>
      <UICard.Header
        title="Card with Title"
        subtitle="A long description goes in this part"
      >
        <UILegends
          legendsData={[
            { color: "supportive500", label: "Avg Legend 1", shape: "line" },
            { color: "primary500", label: "Avg Legend 2", shape: "dot" },
          ]}
        />
      </UICard.Header>
      <UICard.Content>
        <UIText tag="body2">Here goes a graph.</UIText>
      </UICard.Content>
    </UICard>
  </Story>
</Preview>
