import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import UIProvider from "../../ui/UIProvider/UIProvider";
import Chart from "./Chart";
import developmentTimelineSchema from "../../examples/DevelopmentTimeline/DevelopmentTimeline.schema.ts";
import developmentTimelineData from "../../examples/DevelopmentTimeline/DevelopmentTimeline.mockup.json";
import historicalRentSchema from "../../examples/HistoricalRent/HistoricalRent.schema.ts";
import historicalRentData from "../../examples/HistoricalRent/HistoricalRent.mockup.json";
import concessionsSchema from "../../examples/Concessions/Concessions.schema.ts";
import concessionsData from "../../examples/Concessions/Concessions.mockup.json";
import concessionsQuarterSchema from "../../examples/ConcessionsQuarter/ConcessionsQuarter.schema.ts";
import concessionsQuarterMockup from "../../examples/ConcessionsQuarter/ConcessionsQuarter.mockup.json";

<Meta
  title="LittCharts/Chart"
  decorators={[
    (storyFn) => (
      <UIProvider theme="thanos">
        <div style={{ height: "355px" }}>{storyFn()}</div>
      </UIProvider>
    ),
  ]}
  argTypes={{
    schema: {
      name: "Schema",
      description:
        "Helps the component to interpret how you want to render the data.",
    },
  }}
/>

# Chart

## Description

<ArgsTable of={Chart} />

## Examples

export const Template = (args) => <Chart {...args} />;

<Canvas>
  <Story
    name="Development Timeline Example"
    args={{
      schema: developmentTimelineSchema,
      data: developmentTimelineData,
      isLoading: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Historical Rent Example"
    args={{
      schema: historicalRentSchema,
      data: historicalRentData,
      isLoading: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Concessions Example"
    args={{
      schema: concessionsSchema,
      data: concessionsData,
      isLoading: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Concessions Quarter Example"
    args={{
      schema: concessionsQuarterSchema,
      data: concessionsQuarterMockup,
      isLoading: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
